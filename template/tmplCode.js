//模板源的源接口基础格式，需要配合对应的模板源
let parse = {
    作者: "",
    /*//此为自定义解析逻辑，需要则去除注释
    解析: function(url){
        appconfig = parse.配置;
        let {url,parse_api_url,token,from} = JSON.parse(url);
        ...
    }
    //其他需要属性也是一样，需要的可以自己写，会加载覆盖到模板中
    */
};